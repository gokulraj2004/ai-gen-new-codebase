/**
 * EXAMPLE TYPES - Demonstrates TypeScript interface patterns.
 * DELETE this file and create your own domain types.
 *
 * NOTE: The canonical types are in ./index.ts. These are kept only as
 * documentation examples and re-export from the canonical source.
 */

// Re-export canonical types for backwards compatibility
export type { Tag, Item, ItemCreateRequest, ItemUpdateRequest } from './index';